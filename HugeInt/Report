在大整数运算中，我封装了一个大整数的类，并且实现了+ - * \四则运算，以及 > < = >= <=以及<<运算符的重载
尽量规避了000232434等特殊情况，算是一个比较完备的大整数类
加法与减法的预算都是O（n)的，而乘法和除法运算最开始实现的是n的平方的方法，但是离实验要有的结果有很大的距离，中间曾经尝试在int型
的数组里每一个位不是存一位，而是存多位，这样可以减少循环的次数，但是依然是n方的时间复杂度。
最后尝试用FFT实现乘法
但是由于类中不能存放过大的数组，会栈溢出，所以只能破坏原有的封装，但是已经实现了FFT,并且可以得到较好的结果。
